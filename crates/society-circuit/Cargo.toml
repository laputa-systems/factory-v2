[package]
name = "society-circuit"
version = "0.1.0"
edition = "2024"
license = "MIT"

# The parser remains isolated until its observations can be admitted through
# named evidence commands in the kernel.
[workspace]

[dependencies]
society-content = { path = "../society-content" }
thiserror = "=2.0.20"

[lints.rust]
unsafe_code = "deny"
unused_must_use = "deny"

[lints.clippy]
dbg_macro = "deny"
todo = "deny"
undocumented_unsafe_blocks = "deny"
unwrap_used = "warn"
