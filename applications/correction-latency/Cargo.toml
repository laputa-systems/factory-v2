[workspace]
members = ["correction-latency-world", "correction-latency-harness"]
resolver = "3"

[workspace.package]
version = "0.1.0"
edition = "2024"
license = "MIT"

[workspace.dependencies]
society-kernel = { path = "../../crates/society-kernel" }
society-pi = { path = "../../crates/society-pi" }
